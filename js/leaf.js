var animIcons = lottie.loadAnimation({
  container: document.getElementById('reveal_leaf'),
  renderer: 'svg',
  loop: true,
  autoplay: true,
  path: '../images/data.json' // the path to the animation json
});